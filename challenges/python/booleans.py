import sys

# Create all the required variables with their initial state
python_is_glorious = True
failure_is_option = False
proper_greeting = False

# If the second argument is X then do
# Second argument because the first is the file name
if sys.argv[1] == "For the glory of Python!":
    proper_greeting = True
